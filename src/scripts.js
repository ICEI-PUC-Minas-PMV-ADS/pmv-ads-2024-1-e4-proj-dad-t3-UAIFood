$(document).ready(function(){
  $('.slider').slick({
    dots: true,
    autoplay: true,
    autoplaySpeed: 100,
    arrows: false,
    infinite: true,
    speed: 1000,
    fade: true,
    cssEase: 'linear'
  });
});
